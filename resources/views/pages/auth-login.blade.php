@extends('layouts.app')@section('title','Login — SobatAnak')@section('content')
<section class="bg-gradient-to-br from-[#D0F0ED] via-white to-[#FDECEA] py-16"><div class="max-w-xl mx-auto px-6"><div class="card p-8"><span class="text-coral font-black uppercase tracking-widest text-xs">Masuk Akun</span><h1 class="font-display text-4xl mt-3">Login SobatAnak</h1><p class="text-[#6B8A88] font-bold mt-2">Login agar poin, cart, dan reward tersimpan berbeda untuk tiap user.</p>@if($errors->any())<div class="mt-4 rounded-2xl bg-red-50 border border-red-200 p-4 text-red-600 font-bold">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('login.post') }}" class="mt-6 space-y-4">@csrf<input name="email" type="email" value="{{ old('email') }}" class="auth-input" placeholder="Email" required><input name="password" type="password" class="auth-input" placeholder="Password" required><button class="btn-pill btn-coral w-full justify-center">Login</button></form><p class="text-center mt-5 font-bold text-[#6B8A88]">Belum punya akun? <a class="text-coral" href="{{ route('register') }}">Register di sini</a></p></div></div></section>
@endsection
