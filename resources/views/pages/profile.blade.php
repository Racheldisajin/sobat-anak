@extends('layouts.app')@section('title','Profile — SobatAnak')@section('content')
<section class="bg-gradient-to-br from-[#D0F0ED] via-white to-[#FDECEA] py-14"><div class="max-w-7xl mx-auto px-6 md:px-12"><span class="text-coral font-black uppercase tracking-widest text-xs">Profile</span><h1 class="font-display hero-title mt-3">Halo, <span class="text-teal">{{ $user->name }}</span></h1><p class="text-[#6B8A88] font-bold mt-2">Data akun, poin, dan reward di halaman ini khusus akun kamu.</p></div></section>
<section class="max-w-7xl mx-auto px-6 md:px-12 py-12"><div class="grid md:grid-cols-3 gap-5"><div class="card p-6"><div class="profile-avatar big">{{ strtoupper(substr($user->name,0,1)) }}</div><h2 class="font-display text-2xl mt-4">{{ $user->name }}</h2><p class="text-[#6B8A88] font-bold">{{ $user->email }}</p><div class="flex flex-wrap gap-3 mt-5"><a href="{{ route('cart.index') }}" class="btn-pill btn-teal">🛒 Buka Cart</a><form method="POST" action="{{ route('logout') }}">@csrf<button class="btn-pill btn-coral">Logout</button></form></div></div><div class="card p-6"><span class="text-coral font-black uppercase text-xs">Poin Kamu</span><h2 class="font-display text-5xl mt-3">⭐ {{ number_format($point->points,0,',','.') }}</h2><p class="text-[#6B8A88] font-bold mt-3">Poin bertambah dari mini game dan berkurang kalau kamu menukar reward.</p></div><div class="card p-6"><span class="text-coral font-black uppercase text-xs">Cart Kamu</span><h2 class="font-display text-5xl mt-3">🛍️ {{ $cartItems->sum('quantity') }}</h2><p class="text-[#6B8A88] font-bold mt-3">Keranjang dipisah di halaman Cart supaya checkout tidak gabung dengan profile.</p><a href="{{ route('cart.index') }}" class="btn-pill btn-coral mt-5">Lihat Cart</a></div></div><div class="grid md:grid-cols-1 gap-5 mt-8"><div class="card p-6"><h2 class="font-display text-3xl mb-5">Reward Ditukar</h2>@forelse($claims as $claim)<div class="border-b border-[#D4EEEC] py-3"><b>{{ $claim->reward_name }}</b><p class="text-sm text-[#6B8A88]">-{{ number_format($claim->points_used,0,',','.') }} poin · {{ $claim->created_at->format('d M Y H:i') }}</p></div>@empty<p class="text-[#6B8A88] font-bold">Belum ada reward yang ditukar.</p>@endforelse</div></div></section>
@endsection
