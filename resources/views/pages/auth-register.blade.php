@extends('layouts.app')@section('title','Register — SobatAnak')@section('content')
<section class="bg-gradient-to-br from-[#FDECEA] via-white to-[#D0F0ED] py-16"><div class="max-w-xl mx-auto px-6"><div class="card p-8"><span class="text-coral font-black uppercase tracking-widest text-xs">Buat Akun</span><h1 class="font-display text-4xl mt-3">Register SobatAnak</h1><p class="text-[#6B8A88] font-bold mt-2">Setiap akun otomatis punya poin awal dan keranjang sendiri.</p>@if($errors->any())<div class="mt-4 rounded-2xl bg-red-50 border border-red-200 p-4 text-red-600 font-bold">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('register.post') }}" class="mt-6 space-y-4">@csrf<input name="name" value="{{ old('name') }}" class="auth-input" placeholder="Nama lengkap" required><input name="email" type="email" value="{{ old('email') }}" class="auth-input" placeholder="Email" required><input name="password" type="password" class="auth-input" placeholder="Password minimal 6 karakter" required><input name="password_confirmation" type="password" class="auth-input" placeholder="Konfirmasi password" required><button class="btn-pill btn-coral w-full justify-center">Daftar Sekarang</button></form><p class="text-center mt-5 font-bold text-[#6B8A88]">Sudah punya akun? <a class="text-coral" href="{{ route('login') }}">Login</a></p></div></div></section>
@endsection
